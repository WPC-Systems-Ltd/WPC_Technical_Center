---
title: 狀態列為 IP Inconsistent
sidebar_label: 狀態列為 IP Inconsistent
sidebar_position: 4
---

## 問題描述

在 WDM 中可以搜尋到裝置，但狀態列顯示為 **"IP inconsistent"**，導致無法進行連線或操作。

## 可能原因

此狀態表示 **電腦端網卡** 與 **WPC 裝置** 位於 **不同的網域**。

*   **相同網域範例 (正確)**：
    *   電腦：`192.168.1.20`
    *   裝置：`192.168.1.110`
*   **不同網域範例 (錯誤)**：
    *   電腦：`192.168.1.20`
    *   裝置：`192.168.0.110` 或 `192.168.2.110`

## 解決方案

1.  **確認網段設定**：請確保負責連線的電腦網卡 IP 與裝置 IP 位於同一子網域。
2.  **使用靜態 IP (Static IP)**：建議暫時關閉 DHCP，手動指定電腦網卡的 IP 位址與子網路遮罩 (Subnet Mask)，以確保連線穩定。

    *   **WPC 裝置設定範例**：IP `192.168.1.110` / Mask `255.255.255.0`
    *   **電腦端設定範例**：IP `192.168.1.20` / Mask `255.255.255.0`
